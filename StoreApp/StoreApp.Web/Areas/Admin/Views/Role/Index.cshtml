@model List<IdentityRole>

<div class="text-center">
  <div class="display-6 mb-3">Roles</div>
</div>

<ul class="list-group">
    @foreach (var role in Model)
    {
      <li class="list-group-item">@role.Name</li>
    }
</ul>
